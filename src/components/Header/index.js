import React from "react";
import "./style.css";

function Header(){
    return (
        <header>
            <h1>Hello There</h1>
            <h2>Click an Obi-Wan to earn points,<br/>but don't click on any more than once!</h2>
        </header>
    );
}

export default Header;